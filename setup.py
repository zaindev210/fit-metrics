from setuptools import setup, find_packages

setup(
    name="fitmetrics",
    version="0.1.0",
    description="A Python-based fitness assessment desktop application",
    author="Malik Zain Ali",
    packages=find_packages(),
    )
# -----version increment----
# 0.1.0 → early development
# 0.2.0 → more features
# 1.0.0 → first stable release